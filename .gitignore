# Python
.venv/
__pycache__/
*.pyc

# Local secrets / credentials — never commit these
.env
*.token
.garminconnect/
.staticrypt.json   # salt/hash dropped by StatiCrypt during the Pages build

# NOTE: the garmin/ data folder IS committed on purpose (Path A relies on it).
